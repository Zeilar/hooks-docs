import UseGlobalEventDemo from "@site/src/components/demo/UseGlobalEventDemo";

Press any key

<UseGlobalEventDemo />

```tsx
import { useGlobalEvent } from "use-ful-hooks-ts";
import React, { useState } from "react";

function UseGlobalEvent() {
	const [key, setKey] = useState<string>();
	useGlobalEvent("keydown", e => setKey(e.key));
	return <pre>{key ?? "No key pressed."}</pre>;
}
```
